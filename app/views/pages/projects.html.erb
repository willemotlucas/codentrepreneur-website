<section id="free_projects">
	<div class="container">
		<div class="row">
			<h1>Projets gratuits</h1>
			<%= render partial: 'common/free_project_card' if @free_projects.count > 0 %>
		</div>
	</div>
</section>

<section id="paid_projects">
	<div class="container">
		<div class="row">
			<h1>Projets payants</h1>
			<%= render partial: 'common/paid_project_card' if @paid_projects.count > 0 %>
		</div>
	</div>
</section>

<section id="preorder_projects">
	<div class="container">
		<div class="row">
			<h1>Projets disponibles en pré-commande</h1>
				<%= render partial: 'common/preorder_project_card' if @preorder_projects.count > 0 %>
		</div>
	</div>
</section>
