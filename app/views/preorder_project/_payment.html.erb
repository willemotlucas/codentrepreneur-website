<%= form_tag("/preorder_project/" + @preorder_project.friendly_id + "/payment", method: :post, id: "payment-form") do %>

  <input type="hidden" name="preorder_project_id" value="<%= @preorder_project.id %>">

  <%= render partial: 'common/payment_form', object: @preorder_project, as: "project" %>

<% end %>